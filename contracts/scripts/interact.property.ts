import { ethers } from "hardhat";

async function main() {
  const contractAddress: string | undefined = "";
  const abi = [];
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
